[package]
name = "backtester"
version = "0.1.0"
edition = "2021"

[dependencies]
csv = "1.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
rayon = "1.10"

[profile.release]
opt-level = 3
lto = true
