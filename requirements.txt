# Core
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.15.0
streamlit>=1.28.0

# Data
yfinance>=0.2.0
python-dotenv>=1.0.0

# Broker (optional — only if using Kite Connect)
# kiteconnect>=5.0.0

# Rust build tool (optional — only for building rust_mp)
# maturin>=1.5

# Dev tools (optional)
# black>=23.0
# ruff>=0.1.0
# pytest>=7.0
